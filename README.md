# WebExplorer
Web Explorer is a lightweight, web-based file manager for Windows that runs locally via Node.js. It features a dual-panel layout, tabbed browsing, drag-and-drop file operations, context menu, image preview, and a clean dark UI inspired by Windows 11 — all accessible from your browser at localhost:3000.
